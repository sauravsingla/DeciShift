contract:
  max_decision_shift_rate: 0.05
  max_0_to_1_rate: 0.05
  max_1_to_0_rate: 0.03

  attribution:
    require_reproducible_identity: true
    max_score_efficiency_mae: 0.001

  approximate_attribution:
    max_decision_ci_width: 0.10

  cohorts:
    min_size: 100
    max_flip_rate: 0.12
