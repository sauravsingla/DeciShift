# Equipment-maintenance example

The built-in `decishift demo` generates a deterministic synthetic equipment-maintenance dataset and changes the feature transformation, model, threshold, and deterministic rule versions. It is deliberately general-purpose and contains no fraud, AML, payment, money-mule, or employer-specific data.

Run:

```bash
decishift demo
```
